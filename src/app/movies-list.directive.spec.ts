import { MoviesListDirective } from './movies-list.directive';

describe('MoviesListDirective', () => {
  it('should create an instance', () => {
    const directive = new MoviesListDirective();
    expect(directive).toBeTruthy();
  });
});
